# Copy to .env and fill in. The system runs fully offline without a key
# (deterministic fallbacks kick in) but the LLM layer needs it for live extraction.
ANTHROPIC_API_KEY=
TRINETRA_OFFLINE=false        # set true to force all offline fallbacks (demo insurance)
